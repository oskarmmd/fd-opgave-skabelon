---
import VisionIMG from "@assets/images/team1.jpg"; 
import { Image } from "astro:assets";
---

<section class="vision">
  <div class="wrap">
    <div class="grid">
      <div class="copy">
        <p class="eyebrow">OUR VISION</p>
        <h2 class="title">Turn your ideas into reality.</h2>

        <p class="lead">
          Capitalize on low hanging fruit to identify a ballpark value added activity
          beta test. Override the digital divide with additional from DevOps.
        </p>

        <ul class="list">
          <li class="item">
            <span class="check">✓</span>
            <span>Bring to the table win-win survival strategies to ensure proactive domination.</span>
          </li>
          <li class="item">
            <span class="check">✓</span>
            <span>Capitalize on low hanging fruit to identify a ballpark value added activity to beta test.</span>
          </li>
        </ul>
      </div>

      <div class="media">
        <div class="media-inner">
          <Image src={VisionIMG} alt="Vision" />
        </div>
      </div>
    </div>

    <div class="stats">
      <div class="stat">
        <p class="value">1128+</p>
        <p class="label">Successful Work</p>
      </div>
      <div class="stat">
        <p class="value">908+</p>
        <p class="label">Team member</p>
      </div>
      <div class="stat">
        <p class="value">258+</p>
        <p class="label">Happy Customers</p>
      </div>
      <div class="stat">
        <p class="value">564+</p>
        <p class="label">Creative Idea</p>
      </div>
    </div>
  </div>
</section>

<style>
  .vision { padding: 80px 0; background: #fff; }

  .wrap { max-width: 1180px; margin: 0 auto; padding: 0 24px; }

  .grid {
    display: grid;
    grid-template-columns: 1.2fr 0.8fr;
    gap: 64px;
    align-items: start;
  }

  .eyebrow {
    margin: 0 0 10px;
    font-size: 12px;
    letter-spacing: .18em;
    text-transform: uppercase;
    color: #6b7280;
  }

  .title {
    margin: 0 0 14px;
    font-size: 44px;
    line-height: 1.1;
    font-weight: 800;
    color: #111827;
  }

  .lead {
    margin: 0 0 18px;
    max-width: 55ch;
    color: #6b7280;
    line-height: 1.6;
    font-size: 14px;
  }

  .list { list-style: none; padding: 0; margin: 18px 0 0; display: grid; gap: 14px; }
  .item { display: grid; grid-template-columns: 22px 1fr; gap: 12px; color: #6b7280; font-size: 13px; line-height: 1.55; }

  .check {
    width: 18px; height: 18px;
    border-radius: 999px;
    display: grid; place-items: center;
    font-size: 12px; font-weight: 900;
    color: #16a34a;
    border: 2px solid rgba(22,163,74,.35);
    margin-top: 2px;
  }

  .media { display: flex; justify-content: flex-end; }

 .media-inner {
  position: relative;
  width: 420px;
  max-width: 100%;
  aspect-ratio: 1.18 / 1;
  z-index: 0; 
}


  .media-inner::before {
    content:"";
    position:absolute;
    right:-20px; top:-20px;
    width:86px; height:86px;
    background:#63b35b;
    border-radius:18px;
    z-index:-1;
  }

  .media-inner::after {
    content:"";
    position:absolute;
    right:-50px; top:-50px;
    width:180px; height:180px;
    background-image: radial-gradient(rgb(202, 140, 7) 1.3px, transparent 1.3px);
    background-size: 12px 12px;
    opacity:.45;
    z-index:-1;
  }

  .img {
    position: relative; z-index: 1;
    width: 100%; height: 100%;
    object-fit: cover;
    border-radius: 18px;
    box-shadow: 0 20px 45px rgba(17,24,39,.12);
    display:block;
  }

  .stats {
    margin-top: 60px;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 22px;
    text-align: center;
  }

  .value { margin: 0 0 6px; font-size: 42px; font-weight: 800; color: #111827; }
  .label { margin: 0; font-size: 12px; font-weight: 600; color: #6b7280; }

@media (max-width: 768px) {
  .grid {
    grid-template-columns: 1fr;
    gap: 32px;
  }

  /* 🔥 skjul billedsektionen helt */
  .media {
    display: none;
  }

  .title {
    font-size: 32px;
  }

  .lead {
    font-size: 13px;
    max-width: 100%;
  }

  .stats {
    grid-template-columns: repeat(2, 1fr);
    gap: 24px;
    margin-top: 40px;
  }

  .value {
    font-size: 32px;
  }
}

@media (max-width: 420px) {
  .stats {
    grid-template-columns: 1fr 1fr;
  }
}

</style>
